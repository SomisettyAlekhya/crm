# This project runs fully WITHOUT any API key or database credentials.
# The lines below are provided only so you know what to set if/when you
# want to swap the mock LLM for a real Groq call (see app/agent/llm.py).
#
# GROQ_API_KEY=your_groq_api_key_here
